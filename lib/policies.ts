/**
 * Owner's call (2026-09-23): the three policy pages stay fully written and
 * translated, but aren't linked anywhere yet — their titles show as plain
 * text in the footer and they're left out of the sitemap. Flip to true to
 * make every policy link clickable again; nothing else needs to change.
 */
export const POLICY_LINKS_ENABLED = false;
